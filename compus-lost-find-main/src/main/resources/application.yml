server:
  port: 8081

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/campus_lost_found?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: zjmgzh2@
  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 10MB

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

jwt:
  secret: campus-lost-found-secret-key-2024-zhangjiaming-springboot
  expiration: 86400000

# 阿里云 OSS 配置（头像/物品图片上传）
aliyun:
  oss:
    endpoint: https://oss-cn-beijing.aliyuncs.com
    bucket-name: compus-lost-find
    region: cn-beijing
    avatar-dir: avatar
    images-dir: images
    # 凭证二选一：yml 填写，或设置环境变量 ALIBABA_CLOUD_ACCESS_KEY_ID / ALIBABA_CLOUD_ACCESS_KEY_SECRET
    access-key-id:
    access-key-secret:

# 事务管理日志
logging:
  level:
    org.springframework.jdbc.support.JdbcTransactionManager: debug
